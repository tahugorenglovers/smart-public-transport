# Minimal dev/demo configuration.
# allow_anonymous true is fine here because the broker is only reachable
# inside the docker-compose network (and on localhost:1883 if you expose
# it) - do NOT use this config if you ever expose port 1883 publicly.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
